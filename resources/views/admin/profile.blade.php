<div id="profile">
    <div><a class="dropdown-toggle" data-toggle="dropdown"><img src="#" alt="{{ Auth::user()->name }}" title="{{ Auth::user()->name }}" class="img-circle" /></a></div>
    <div>
        <h4>{{ Auth::user()->name }}</h4>
        <small>{{ Auth::user()->name }}</small></div>
</div>